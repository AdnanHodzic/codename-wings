# Golang Server WebApp

Starts a HTTP server on port 80 with web app that has basic routing functionality, i.e:

```http://localhost``
```http://localhost/homersimpson```
```http://localhost/covilha```

# Golang run example:

```
cd gofiles/; go run main.go
```

## Docker build example:

```
make v=0.1
```

# Docker run example:

```
docker run -d -p 80:80 docker.io/adnanhodzic/golang-server-webapp:0.1
```
